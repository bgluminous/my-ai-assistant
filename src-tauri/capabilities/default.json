{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window and tray panel",
  "windows": ["main", "tray"],
  "permissions": ["core:default"]
}
